---

---
<section class="panel">
    <h2>Settings</h2>
    <div class="row">
        <label class="field">
            <span class="muted">Exclude triggers (comma separated)</span>
            <input id="settingsTriggers" type="text" placeholder="e.g. calorie, self-harm, alcohol" />
        </label>
        <button class="pill" id="saveSettings" type="button">Save</button>
    </div>
</section>